<p id="notice"><%= notice %></p>

<p>
  <strong>Descrição:</strong>
  <%= @position.description %>
  <br>
  <strong>Sub grupo:</strong>
  <%= @position.subgroup.description %>
</p>

<%= link_to 'Editar', edit_position_path(@position) %> |
<%= link_to 'Voltar', positions_path %>
